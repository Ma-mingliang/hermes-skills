---
name: watchers
description: 轮询监控 RSS、JSON API、GitHub Releases, 支持水印去重、变更检测、定时触发。
allowed-tools: Bash(python:*), Bash(curl:*), Bash(wget:*)
---

# watchers

## 分类

B类-任务执行

## 功能

多源监控能力:
1. RSS/Atom feed轮询
2. JSON API定时查询
3. GitHub Release/Commit监听
4. 内容去重(水印机制)
5. 变更推送

## 使用方式

在任务中调用此 skill 以启用对应能力。
